/*
Deployment script for Database

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Database"
:setvar DefaultFilePrefix "Database"
:setvar DefaultDataPath "C:\Users\HP\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\mssqllocaldb\"
:setvar DefaultLogPath "C:\Users\HP\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\mssqllocaldb\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Location].[Date] on table [dbo].[Location] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The type for column Latitude in table [dbo].[Location] is currently  DATETIME NOT NULL but is being changed to  FLOAT (53) NOT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Location])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'The following operation was generated from a refactoring log file 10c4eda5-a8a7-4f5f-afbc-c4fda518ccf8';

PRINT N'Rename [dbo].[Location].[Date] to Latitude';


GO
EXECUTE sp_rename @objname = N'[dbo].[Location].[Date]', @newname = N'Latitude', @objtype = N'COLUMN';


GO
PRINT N'Starting rebuilding table [dbo].[Location]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Location] (
    [OrderId]   INT            NOT NULL,
    [TypeId]    INT            NOT NULL,
    [Quantity]  INT            NOT NULL,
    [Address]   NVARCHAR (MAX) NOT NULL,
    [Longitude] FLOAT (53)     NOT NULL,
    [Latitude]  FLOAT (53)     NOT NULL,
    [Date]      DATETIME       NOT NULL
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Location])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_Location] ([OrderId], [TypeId], [Quantity], [Address], [Longitude], [Latitude])
        SELECT [OrderId],
               [TypeId],
               [Quantity],
               [Address],
               [Longitude],
               CAST ([Latitude] AS FLOAT (53))
        FROM   [dbo].[Location];
    END

DROP TABLE [dbo].[Location];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Location]', N'Location';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
-- Refactoring step to update target server with deployed transaction logs
IF NOT EXISTS (SELECT OperationKey FROM [dbo].[__RefactorLog] WHERE OperationKey = '10c4eda5-a8a7-4f5f-afbc-c4fda518ccf8')
INSERT INTO [dbo].[__RefactorLog] (OperationKey) values ('10c4eda5-a8a7-4f5f-afbc-c4fda518ccf8')

GO

GO
PRINT N'Update complete.';


GO
